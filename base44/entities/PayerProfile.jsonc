{
  "name": "PayerProfile",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "title": "Payer Name"
    },
    "payer_type": {
      "type": "string",
      "enum": [
        "Medicare",
        "BCBS",
        "Auto/PI",
        "Cash",
        "Other Commercial"
      ],
      "title": "Payer Type"
    },
    "enabled_codes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "title": "Enabled Codes"
    },
    "disabled_codes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "title": "Disabled Codes"
    },
    "covered_treatment_alternatives": {
      "type": "array",
      "title": "Covered Treatment Alternatives",
      "items": {
        "type": "object",
        "properties": {
          "excluded_code": { "type": "string", "title": "Excluded / Non-Covered CPT" },
          "alternative_code": { "type": "string", "title": "Potential Covered CPT" },
          "alternative_description": { "type": "string", "title": "Alternative Treatment" },
          "coverage_note": { "type": "string", "title": "Coverage Note" },
          "verification_source": { "type": "string", "title": "Verification Source" }
        }
      }
    },
    "default_modifiers": {
      "type": "string",
      "title": "Default Modifiers"
    },
    "warning_notes": {
      "type": "string",
      "title": "Warning Notes"
    },
    "billing_notes": {
      "type": "string",
      "title": "Office Billing Notes"
    },
    "documentation_reminders": {
      "type": "string",
      "title": "Documentation Reminders"
    },
    "plan_comments": {
      "type": "string",
      "title": "Plan-Specific Comments"
    },
    "claim_type_default": {
      "type": "string",
      "title": "Default Claim Type"
    },
    "active": {
      "type": "boolean",
      "title": "Active",
      "default": true
    },
    "description": {
      "type": "string",
      "title": "Description",
      "maxLength": 1000
    }
  },
  "required": [
    "name",
    "payer_type"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "user"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}
